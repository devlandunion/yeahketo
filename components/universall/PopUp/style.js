import css from "styled-jsx/css"

export default css`
  .modal {
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.4);
    position: fixed;
    z-index: 125;
    display: flex;
    justify-content: center;
  }
`
